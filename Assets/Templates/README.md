# 🖼️ Visual Templates

Editable design templates (slide masters, banner templates, etc.) — distinct from the top-level [`Templates/`](../../Templates/) folder, which holds *content* templates (Customer Success Plan, Health Scorecard, etc.), not visual design files.

## Status

🔴 **Not started.**

## What's Needed

- A branded PowerPoint slide master matching the visual system used in `Training Videos/Scripts/` decks (Navy `#1B2A4A` / Amber `#E08A3C`, Space Grotesk + Inter), so future decks don't need to be rebuilt from scratch.
- A blank certificate/banner design source file, if the team wants to edit outside HTML (see `Assets/Certificates/` and `Assets/Images/` for the current HTML-based sources).
